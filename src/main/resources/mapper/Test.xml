<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 定义MyBatis接口映射器 -->
<mapper namespace="org.chench.springboot.falsework">
    <select id="getTestList" resultType="org.chench.springboot.falsework.model.Test">
        SELECT * FROM test LIMIT #{start}, #{offset}
    </select>
</mapper>